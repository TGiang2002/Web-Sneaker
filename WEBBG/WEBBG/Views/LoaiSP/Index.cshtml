@model IEnumerable<WEBBG.Models.LOAISP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h3 style="text-align:center;color:red;"> QUẢN LÝ LOẠI SẢN PHẨM</h3>

<p>
    @Html.ActionLink("THÊM LOẠI SẢN PHẨM", "Create")
</p>
<table class="table">
    <tr>
        <th>
            TÊN LOẠI
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENLOAI)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MALOAI }) |
            @Html.ActionLink("Details", "Details", new { id=item.MALOAI }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MALOAI })
        </td>
    </tr>
}

</table>
